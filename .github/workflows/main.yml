on: push
name: 🚀 Enviar a producción 
jobs:
  web-deploy:
    name: 🎉 Enviar
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Obteniendo cambios
      uses: actions/checkout@v3
    
    - name: 📂 Sincronizando archivos 
      uses: SamKirkland/FTP-Deploy-Action@4.3.3
      with:
        server: ftp.hotelnickrivieramaya.com
        server-dir: public_html/  # Remote directory where files will be uploaded
        local-dir: dist/           # Sync only the 'dist' folder
        username: hotelni2
        password: ${{ secrets.ftp_password }}
